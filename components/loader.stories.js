import Loader from './loader'

export default {
  title: 'Simulator/Loader',
  component: Loader,
  tags: ['autodocs'],
  argTypes: {
    onClick: { action: 'click' },
  },
};

export const Primary = {}
